import 'package:flutter/material.dart';

class CartScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Your explore screen implementation
    return Container(
      color: Colors.green,
      child: const Center(
        child: Text('Explore Screen'),
      ),
    );
  }
}